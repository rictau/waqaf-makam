rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /proofs/{allPaths=**} {
      // Allow get to generate and access tokenized download URL, but forbid directory listing
      allow get: if true;
      allow list: if false;

      // Anyone can upload a proof file, but only if it's an image or PDF and less than or equal to 10MB
      allow write: if request.resource.size <= 10 * 1024 * 1024
                   && (request.resource.contentType.matches('image/.*') || request.resource.contentType == 'application/pdf');
    }

    match /campaigns/{allPaths=**} {
      // Anyone can view campaign images/banners
      allow read: if true;

      // Images up to 10MB can be uploaded by authenticated admins
      allow write: if request.auth != null
                   && request.resource.size <= 10 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
